package com.trafik.teklif_api.mapper;

import org.junit.jupiter.api.Test;
import static org.assertj.core.api.Assertions.assertThat;

class DtoMappersTest {

  @Test
  void quote_to_response_mapping() {
    // Quote → QuoteResponse mapper’ını çağır ve alanların birebir aktarıldığını doğrula
    // assertThat(resp.getVehicleId()).isEqualTo(quote.getVehicle().getId());
  }
}
